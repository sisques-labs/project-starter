name: Docs Build

on:
  pull_request:
    paths:
      - 'apps/docs/**'
    branches: [main, dev]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '24'

      - name: Install pnpm
        uses: pnpm/action-setup@v2
        with:
          version: latest

      - name: Install dependencies
        run: |
          if [[ "${{ github.actor }}" == "dependabot[bot]" ]]; then
            pnpm install --no-frozen-lockfile --filter=./apps/docs...
          else
            pnpm install --frozen-lockfile --filter=./apps/docs...
          fi

      - name: Build the docs
        run: pnpm build --filter=./apps/docs...
