name: Admin Build

on:
  pull_request:
    paths:
      - 'apps/admin/**'
    branches: [main, dev]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '24'

      - name: Install pnpm
        uses: pnpm/action-setup@v2
        with:
          version: latest

      - name: Install dependencies
        run: |
          if [[ "${{ github.actor }}" == "dependabot[bot]" ]]; then
            pnpm install --no-frozen-lockfile --filter=./apps/admin...
          else
            pnpm install --frozen-lockfile --filter=./apps/admin...
          fi

      - name: Lint code
        run: pnpm lint --filter=./apps/admin...

      - name: Run unit tests
        run: pnpm test --filter=./apps/admin...

      - name: Build the app
        run: pnpm build --filter=./apps/admin...
