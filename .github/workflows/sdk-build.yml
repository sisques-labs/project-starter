name: SDK Build

on:
  pull_request:
    paths:
      - 'packages/sdk/**'
    branches: [main, dev]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '24'

      - name: Install pnpm
        uses: pnpm/action-setup@v2
        with:
          version: latest

      - name: Install dependencies
        run: |
          if [[ "${{ github.actor }}" == "dependabot[bot]" ]]; then
            pnpm install --no-frozen-lockfile --filter=./packages/sdk...
          else
            pnpm install --frozen-lockfile --filter=./packages/sdk...
          fi

      - name: Lint code
        run: pnpm lint --filter=./packages/sdk...

      - name: Check types
        run: pnpm check-types --filter=./packages/sdk...

      - name: Build the SDK
        run: pnpm build --filter=./packages/sdk...
