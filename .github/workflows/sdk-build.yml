name: SDK Build

on:
  pull_request:
    paths:
      - 'packages/sdk/**'
    branches: [main, dev]

jobs:
  build-sdk:
    uses: sisques-labs/workflows/.github/workflows/web-build.yml@main
    with:
      app_path: 'packages/sdk'
      app_name: 'SDK Package'
      node_version: '24'
      run_lint: true
      run_test: false
      build_command: 'build'
    secrets: inherit # Required if the workflow needs secrets
